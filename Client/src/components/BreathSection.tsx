import "./css/breathsection.scss";
import { useState } from "react";

export const BreathSection = () => {
  /** STATES */
  const [healthBool, setHealthBool] = useState<boolean>(true);

  return (
    <div className="breathe-section-container">
      breathe
    </div>
  );
};